{
  "name": "sentient-backend-tooling",
  "private": true,
  "version": "0.1.0",
  "description": "Code formatting hooks for sentient-backend",
  "scripts": {
    "prepare": "husky",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "prettier": "^3.5.3"
  },
  "lint-staged": {
    "*.py": [
      "bash -lc 'if [ -x .venv/bin/python ]; then .venv/bin/python -m ruff format \"$@\" && .venv/bin/python -m ruff check --fix \"$@\"; elif [ -x .venv/Scripts/python.exe ]; then .venv/Scripts/python.exe -m ruff format \"$@\" && .venv/Scripts/python.exe -m ruff check --fix \"$@\"; else echo \"[husky] Missing .venv Python. Run bootstrap/install first.\"; exit 1; fi' --"
    ],
    "*.{md,json,yml,yaml}": [
      "prettier --write"
    ]
  }
}
