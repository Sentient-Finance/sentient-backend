"""Pydantic request/response schemas shared across API versions.

Intended to hold cross-cutting schemas (e.g. pagination envelope, error
responses) that are too generic to live in a single route module.
"""
